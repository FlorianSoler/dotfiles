# Backend
glx-no-stencil = true;
glx-copy-from-front = false;
use-damage = true;
glx-no-rebind-pixmap = true;
# Shadow
shadow = true; 

# Opacity
inactive-opacity = 1;
active-opacity = 1;
frame-opacity = 1;

# Blur
blur:
{
  method: "dual_kawase";
  strength: 10;
};

# Grain
blur-noise = true;
blur-noise-style = "grain";
blur-noise-strength = 0.9;

# Transition
transition-length = 0; 
fade-delta = 0; 
fading = false; 

# Exclude applications
opacity-rule = [ 
    "85:class_g = 'Alacritty'" 
];

# Other
mark-wmwin-focused = true;
mark-ovredir-focused = true;
detect-rounded-corners = true;
detect-client-opacity = true;
vsync = true;
dbe = false;
unredir-if-possible = true;
detect-transient = true;
detect-client-leader = true;
invert-color-include = [ ];

# GLX backend
glx-no-stencil = true;
glx-copy-from-front = false;
use-damage = true
glx-no-rebind-pixmap = true;
